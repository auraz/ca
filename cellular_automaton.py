# -*- coding: utf-8 -*-import initclass CellularAutomaton():        def __init__(self, cells = init.cells, transition = init.transition):        # создаёт новый клеточный автомат с начальным состоянием и функцией перехода, заданной в ините        pass        def next(self):        # одна итерация клеточного автомата        pass        def neighbors(self, x, y):        # возвращает всех соседей данной клетки        pass        def are_neighbors(self, x1, y1, x2, y2):        # проверяет, являются ли заданные две клетки соседними        pass        def cells(self):        # возвращает все клетки клеточного автомата        pass    